input type:touchpad {
    tap enabled
    natural_scroll enabled
}

# Laptop lid handling with external monitor support
bindswitch --reload --locked lid:on exec ~/.config/sway/scripts/lid-close.sh
bindswitch --reload --locked lid:off exec ~/.config/sway/scripts/lid-open.sh
exec_always ~/.config/sway/scripts/check-lid-state.sh
